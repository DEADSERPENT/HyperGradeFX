# HyperGradeFX Requirements
# This addon is designed to work with Blender 3.6+ which includes Python 3.11+

# Core requirements (included with Blender)
# Python >= 3.11

# Optional external dependencies for enhanced functionality
# These are NOT required for basic addon operation

# FFmpeg (external binary - install separately)
# Download from: https://ffmpeg.org/download.html
# Used for: Video encoding and export

# Note: The following Python packages are already included in Blender:
# - bpy (Blender Python API)
# - numpy (via Blender)
# - gpu / gpu_extras (Blender graphics)

# Development dependencies (for contributors only)
# These are NOT needed for addon usage
# pylint>=2.17.0
# flake8>=6.0.0
# black>=23.0.0
# mypy>=1.0.0

# Testing dependencies (optional, for development)
# pytest>=7.3.0
# pytest-cov>=4.1.0

# No additional pip packages required for basic addon functionality
# All dependencies are provided by Blender's bundled Python
